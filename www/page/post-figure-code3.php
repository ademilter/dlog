<figure class="code">
        <pre><code class="language-crystal">require "kemal"

get "/" do
render "views/index.ecr"
end</code></pre>
    <figcaption>Now we are going to edit our shard.ymlfile to add Kemal as a dependency.</figcaption>
</figure>